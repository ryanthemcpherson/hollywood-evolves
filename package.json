{
  "name": "hollywood-evolves",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check": "node scripts/check.mjs",
    "audience:tools": "node scripts/audience-tools.mjs",
    "test": "npm run check && npm run build && node --test tests/*.test.mjs",
    "start": "node server.mjs"
  },
  "devDependencies": {
    "axe-core": "^4.10.3",
    "puppeteer-core": "^25.9.0",
    "vite": "^7.1.3"
  },
  "dependencies": {
    "jose": "^6.2.10"
  }
}
